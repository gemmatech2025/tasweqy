on:
  push:
    branches:
      - main

name:  Deploy website on push
jobs:
  web-deploy:
    runs-on: ubuntu-latest
    steps:
      - name:  Get latest code
        uses: actions/checkout@v3
      - name:  Sync files to server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 147.93.52.107
          username: hyatk_ftp_account@hyatk.net
          password: hyatk_ftp_account@123
          local-dir: ./
          server-dir: backend-tasweqy.hyatk.net/
          exclude: |
            **/.git*
            **/.git/**
            **/.gitignore
            **/node_modules/**
            **/.env
            **/.env.example
            **/composer.lock
            **/package-lock.json
            **/.htaccess
            **/storage/**
